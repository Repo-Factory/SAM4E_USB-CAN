/* 
    const int cdc_line_val = udi_cdc_getc();
        // 
        // unsigned char short_message[USB_PACKET_SIZE_BYTES] = {0,0,0,5};
        // ARE_EQUAL(buffer, short_message)
        if (cdc_line_val == 5)
        {
            TOGGLE_LIGHT();
            // for (int i = 0; i < USB_PACKET_SIZE_BYTES; i++) {
            //     buffer[i] = 0;
            // }
        } 
    volatile unsigned char buffer[USB_PACKET_SIZE_BYTES] = {0, 0, 0, 0};    
*/